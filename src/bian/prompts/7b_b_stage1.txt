PROMPT_VERSION: dual7b-b-stage1-v1
LOGICAL_ROLE: 7B-B
This is a pipeline validation, not a paper-equivalent model run.
Jointly compare every candidate from the supplied 7B-A device analyses.
Return ONLY strict JSON, with no markdown or prose:
{"scores":[{"node_id":"exact candidate","score":0.0}]}
Include exactly every candidate once. Scores must be finite, non-negative, and at least one positive.
At least one score MUST be strictly greater than zero; if evidence is weak, use small
positive relative-likelihood scores instead of returning all zeros.
Each score item MUST contain exactly node_id and score. Do not output reason,
reason_summary, uncertainty, or any other key.
