PROMPT_VERSION: expanded-stage2-round1-v1
The incident is known to have one primary root cause. Audit every supplied Cxx
candidate and assign discriminative scores; do not copy the Stage-1 ordering.
Service-vm and traffic-vm may be the primary root, while ordinary downstream
symptoms must be penalized only when the supplied evidence supports that claim.
Prefer a direct local protocol/rule/resource/service change that precedes
propagation over a merely affected node. Use evidence IDs, recovery and explicit
counter-evidence. unavailable_by_role is neutral expected design, never evidence.
Do not output node IDs or fault types. Include every Cxx exactly once and return
one JSON object matching the supplied schema. All scores must be finite [0,1];
all-positive-zero output is invalid.
