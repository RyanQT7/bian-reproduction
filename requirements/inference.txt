# Project-local Dual-7B inference environment (implementation choice).
transformers==5.14.1
accelerate==1.14.0
safetensors==0.8.0
sentencepiece==0.2.2
json-repair==0.61.7

# Install the CUDA build from the official PyTorch index, not PyPI CPU wheels:
# uv pip install --python .venv/bin/python torch==2.11.0 \
#   --index-url https://download.pytorch.org/whl/cu130
